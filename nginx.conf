user nginx;
worker_processes auto;
error_log /var/log/nginx/error.log warn;
pid /var/run/nginx.pid;

# Pass REACT_APP_ENV through to Nginx so we can choose the shared-ui upstream per env
env REACT_APP_ENV;

events {
  worker_connections 1024;
}

http {
  include /etc/nginx/mime.types;
  default_type application/octet-stream;
  log_format main '$remote_addr - $remote_user [$time_local] "$request" '
                    '$status $body_bytes_sent "$http_referer" '
                    '"$http_user_agent" "$http_x_forwarded_for"';
  access_log /var/log/nginx/access.log main;

  sendfile on;
  keepalive_timeout 65;

  # Select the shared-ui upstream based on environment: prod -> prod branch, otherwise -> ipd/uat
  map $REACT_APP_ENV $shared_ui_host {
    prod pm-storybook-ui-prod.tatvacare.in;
    default pm-storybook-ui-uat.kindmushroom-6f77b425.centralindia.azurecontainerapps.io;
  }
  
  server {
    listen 80;

    location /case_manager/pdf_casemanager_send.php {
      add_header 'Access-Control-Allow-Origin' '*';
      add_header 'Access-Control-Allow-Methods' 'GET, POST, OPTIONS';
      add_header 'Access-Control-Allow-Headers' 'Authorization, Content-Type, X-Requested-With';
      add_header 'Access-Control-Allow-Credentials' 'true';
      proxy_pass https://pm-uat-dhspl-2.tatvacare.in;
    }

    # Proxy shared-ui remoteEntry and assets to avoid CORS by keeping same origin
    location /shared-ui/ {
      # Ensure correct Host and SNI for HTTPS upstream
      proxy_set_header Host $shared_ui_host;
      proxy_set_header X-Forwarded-Proto $scheme;
      proxy_set_header X-Forwarded-Host $host;
      proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
      proxy_buffering off;
      proxy_http_version 1.1;
      proxy_set_header Connection "";
      proxy_ssl_server_name on;
      proxy_ssl_name $shared_ui_host;
      proxy_redirect off;
      proxy_read_timeout 60s;
      proxy_connect_timeout 10s;
      proxy_pass https://$shared_ui_host/;
    }

    location / {
      root /usr/share/nginx/html;
      index index.html index.htm;
      try_files $uri $uri/ /index.html =404;
    }
  }
}
